# Copy to .env (gitignored) and adjust. The backend loads this at startup
# (backend/app.py -> python-dotenv). Real shell env vars take precedence over .env.

# --- Inference (mock vs real model) ---
USE_MOCK=1                 # 1 = mock answers; 0 = call model_adapter.predict
MOCK_DELAY_S=0             # artificial mock latency (s); >0 to see the loading state
MOCK_REVEAL=0             # 1 = mock returns the canned answer instead of a disclaimer

# --- Real model: Qwen3-VL (+ LoRA), used only when USE_MOCK=0 (see model_adapter.py) ---
# Serving mode: leave VLM_URL EMPTY to run the model IN-PROCESS (dev on a big GPU; the
# 8B does NOT fit the 6 GB 4050). Set it to a remote VLM service URL (production
# scale-to-zero GPU path, see vlm_service/) and the backend POSTs {image, question}
# there instead of loading the model. VLM_TIMEOUT is generous to survive a cold start.
VLM_URL=
VLM_TIMEOUT=120
# Auth for the remote VLM call (backend/model_adapter.py):
#   none          the VLM is reachable without auth (RunPod tunnel, docker-compose, dev GPU).
#   gcp_id_token  attach a Google-signed ID token (from the Cloud Run metadata server) so
#                 the backend can call a PRIVATE Cloud Run GPU service — the expensive GPU
#                 endpoint isn't public. Set by scripts/gcloud_deploy_app.sh when --vlm-url
#                 is given; don't set this for local/RunPod.
VLM_AUTH=none
# Who starts/warms the remote VLM (see backend/vlm_provider.py):
#   none      (default) VLM_URL is assumed always-on.
#   cloudrun  Cloud Run's own autoscaler starts an instance on request; warm() just
#             sends an early nudge (GET /api/vlm/warm, called by the frontend on load).
#   runpod    dev only — provisions a RunPod pod on demand (scripts/runpod_up.py).
#             Set by `python app.py --dev --runpod`; don't set this in docker-compose's .env.
VLM_PROVIDER=none
QWEN_MODEL_ID=Qwen/Qwen3-VL-8B-Instruct             # base VLM, downloaded from HF on first run
QWEN_ADAPTER_PATH=checkpoints/qwen3vl-lora-final2    # LoRA adapter dir; '' = base model only
# Opt-in bitsandbytes quantization: none | 8bit | 4bit (4bit = NF4 + double quant,
# bf16 compute). Requires `pip install bitsandbytes`; the loader fails loudly if it
# is requested but missing. Quantized runs keep the LoRA adapter attached (no merge).
QWEN_QUANTIZATION=none
QWEN_MAX_NEW_TOKENS=64                               # generation budget for the answer
QWEN_ANSWER_SUFFIX=" Please answer directly."        # appended to the question (terse answers)

# --- Server / uploads ---
HOST=127.0.0.1           # bind address; compose overrides to 0.0.0.0 in the container
PORT=5000
FLASK_DEBUG=1            # 1 = Flask reloader/debug (local); compose sets 0 in the container
CORS_ORIGINS=*          # dev: '*'. Prod: pin to your frontend origin(s), comma-separated
                        # e.g. https://charts.example.com,https://www.example.com
MAX_UPLOAD_MB=10          # reject larger uploads (keep in sync with frontend MAX_BYTES)
MIN_QUESTION_ALNUM=3      # min alphanumeric chars before a question counts as "real"

# --- Answer cache: short-circuits guard+chart+VLM on a repeat (image, question) ---
# In-memory LRU (single worker); Redis is the prod backend (Phase 3.6). Real answers only.
ANSWER_CACHE_ENABLED=1
ANSWER_CACHE_MAX=512      # max entries before evicting the least-recently-used
ANSWER_CACHE_TTL_S=3600   # entry lifetime in seconds (0 = never expire)

# --- Conversation memory (Phase 5): multi-turn chat store, keyed by conversation_id ---
# History (list of {role,text}) + the sanitized chart image live here so follow-up turns
# re-use the image and prior context. Redis in prod, in-memory dict in dev; fail-open.
CONVERSATION_ENABLED=1
CONVERSATION_TTL_S=1800      # idle expiry in seconds (refreshed on each turn; 0 = never)
CONVERSATION_MAX_TURNS=8     # sliding window: keep the last N user/assistant pairs

# --- Data layer (Phase 3.6): shared Redis backend for cache + rate-limit + VLM budget ---
# EMPTY = in-memory fallbacks (dev / single worker). docker-compose sets this to
# redis://redis:6379/0 so the cache + counters are shared across gunicorn workers and
# survive a restart. Fail-open: an unreachable Redis falls back to in-memory, never errors.
REDIS_URL=

# --- Rate limiting (Phase 3.7): per-IP fixed window on /api/ask (Redis-backed when set) ---
RATELIMIT_ENABLED=1
RATELIMIT_PER_MINUTE=30   # max requests per client IP per minute (0 = unlimited)

# --- VLM daily budget circuit breaker (Phase 3.7): caps real GPU answers/day ---
# A COUNT of real VLM invocations per day — NOT a dollar amount. Past this count,
# /api/ask returns 429 BEFORE touching the GPU (cache/guard/mock still work) so a bot
# can't run up the GPU bill. 0 = unlimited (the local-dev default). This is an app-level
# soft cap on volume, not a spend cap — for an actual $ ceiling, set a GCP Billing
# Budget + alert on the project (see docs/REVIEW_AND_ROADMAP.md §3.7).
VLM_DAILY_BUDGET=0

# --- Layer-2 guard: encoder classifiers (opt-in models via requirements-guard.txt) ---
GUARD_ENABLED=1
GUARD_TOXICITY_THRESHOLD=0.7
GUARD_INJECTION_THRESHOLD=0.8
GUARD_PII_THRESHOLD=0.6
GUARD_TOXICITY_MODEL=original
GUARD_INJECTION_MODEL=protectai/deberta-v3-base-prompt-injection-v2

# --- Layer-3 guard: Llama Guard via Ollama (dev) / vLLM (prod) ---
GUARD_LLM_ENABLED=1                       # ON by default; falls back + WARNS if unavailable
GUARD_LLM_URL=http://localhost:11434      # vLLM/prod: http://guard:8000 (see docs/ARCHITECTURE.md)
GUARD_LLM_MODEL=llama-guard3:1b
GUARD_LLM_TIMEOUT=60                       # generous enough to survive a cold model load — 20
                                            # was too short on Cloud Run's CPU allocation and
                                            # created a self-defeating race: the client gave up
                                            # and closed the connection mid-load, which made
                                            # Ollama ABORT the load and restart from scratch on
                                            # the next request — guard could never finish a cold
                                            # load at all (confirmed via logs, 2026-07-10).
# Auth for the guard call — same idea as VLM_AUTH (backend/gcp_auth.py). none = no header
# (Ollama on localhost/docker-compose); gcp_id_token = private Cloud Run guard service.
GUARD_LLM_AUTH=none
# Production only: HF token for the gated meta-llama/Llama-Guard-3-1B weights vLLM pulls.
# HF_TOKEN=

# --- Required Google sign-in (Phase 3.7): gates /api/ask + /api/vlm/warm ---
# 0 (default) = no login wall, anyone can use /api/ask — the local/--dev/test default.
# Cloud Run sets this to 1: nobody calls /api/ask without a valid Google session (a much
# higher bar against abuse than per-IP rate-limiting alone — accounts aren't free to mint
# at scale). Stateless: the frontend sends a Google ID token as `Authorization: Bearer
# <token>` on every call; the backend verifies its signature per-request (backend/auth.py)
# — no session cookie, no server-side session store.
AUTH_ENABLED=0
# OAuth 2.0 Client ID (Google Cloud Console -> APIs & Services -> Credentials -> "Web
# application"; Authorized JavaScript origins = the frontend's URL). Public value, not a
# secret — required only when AUTH_ENABLED=1. No client secret needed (ID-token
# verification only needs the Client ID as the expected audience).
GOOGLE_CLIENT_ID=

# --- Feedback flywheel (Phase 5): 👍/👎 -> GCS, a fine-tuning data source ---
# 0 (default) = feedback endpoint accepts votes but persists nothing (local/dev/test).
# In prod, set to 1 and FEEDBACK_GCS_BUCKET to a bucket the backend SA can write to; each
# vote appends a JSONL row + stores the chart image. Fire-and-forget, fail-open — a write
# failure never breaks the user's request.
FEEDBACK_ENABLED=0
FEEDBACK_GCS_BUCKET=

# --- Chart detector (Task A): CLIP zero-shot + OCR "has data" gate ---
CHART_CLIP_MODEL=openai/clip-vit-base-patch32
CHART_CLIP_THRESHOLD=0.5                   # P(chart) >= this AND has numeric data -> chart
CHART_MIN_DATA_DIGITS=2                    # min numeric chars OCR must find
# Hard block (no VLM call at all) when the gate is quite sure it's NOT a chart — below
# CHART_CLIP_THRESHOLD but above this still gets a soft "may be unreliable" warning and
# proceeds to the model; below THIS blocks outright. Protects against off-topic images
# reaching the model (2026-07-10: an unrelated photo got a bizarre off-topic answer from
# the VLM, since only the question text is guarded, not the image) and saves a GPU call
# on images that clearly aren't charts. Lower than CHART_CLIP_THRESHOLD on purpose — only
# the confident non-chart cases should hard block; ambiguous ones stay soft-warned.
# 0.3 was too low in practice: a real unrelated photo (shrimp) scored 0.37 confidence —
# clearly "not a chart" to a human, but above 0.3 so it still reached the model and got a
# nonsense answer. 0.4 catches that case while still leaving a 0.4-0.5 gray band for
# genuinely ambiguous images (confirmed 2026-07-10, live test).
CHART_BLOCK_THRESHOLD=0.4
# Pixel-heuristic fallback (used when CLIP/torch is unavailable):
CHART_SAMPLE_SIZE=128
CHART_MIN_BACKGROUND_RATIO=0.18
CHART_MAX_DISTINCT_COLORS=48
# Tesseract OCR engine: leave blank to use PATH (Linux/containers). On Windows
# point at the binary, e.g.:
# TESSERACT_CMD=C:\Program Files\Tesseract-OCR\tesseract.exe
TESSERACT_CMD=
