modeling/checkpoints/**/*.safetensors filter=lfs diff=lfs merge=lfs -text
# Committed topic-check embedding model (all-MiniLM-L6-v2) — served only via HF Xet CDN,
# which fails on Cloud Build's network, so it's vendored here and loaded from disk.
backend/models/**/*.safetensors filter=lfs diff=lfs merge=lfs -text
